int majorityElement(vector<int>& nums) {
        int n=nums.size();
        unordered_map<int,int>ans;
        for(int i=0;i<n;i++){
            ans[nums[i]]++;
            if(ans[nums[i]]>n/2) return nums[i];
        }
        return 0;
    }